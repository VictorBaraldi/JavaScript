// useReducer serve para lidarmos com estados que possuem funções fixas responsáveis por modificar o mesmo
// const [state (estado normal do item), dispatch(função que manda as ações do reducer)] = React.useReducer(função construtora, estado inicial)
